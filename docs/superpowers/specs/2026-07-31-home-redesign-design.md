# allrunabout 홈 리디자인 설계

작성일 2026-07-31 · 대상 브랜치 `redesign-b` · 상태 승인됨

---

## 1. 목적과 범위

홈페이지를 전면 재설계해 allrunabout을 "한국 러너 기준으로 러닝화를 정리하는 플랫폼"으로 인식시키고,
맞춤 추천·상세·비교·베스트·블로그로의 이동을 강화한다.

홈의 임무는 자체 광고 수익이 아니라 **순회 허브**다. 홈은 전체 조회의 10.5%를 차지하지만
직접 광고 수익은 3.7%이고, 신발 상세가 전체 수익의 60~70%를 차지한다.
따라서 홈의 성과는 하위 페이지 이동률과 홈 경유 세션 전체 수익으로 판단한다.

**포함**: 홈 전체 정보구조 · 상단 내비 · 키네틱 히어로 · 고민별 탐색 · 추천 설명 · 신뢰 지표 ·
필터와 러닝화 인덱스 · 재방문 이어보기 · 기존 광고 슬롯 1개 · 에디토리얼 · 최종 CTA ·
반응형 · 접근성 · 성능 · SEO · GA4 이벤트 · 전역 팔레트 토큰 조정

**제외**: 마라톤 지도와 마라톤 페이지 구조 · 상세/비교/베스트/블로그 레이아웃 재설계(후속 단계) ·
새 추천 알고리즘 · 신규 AdSense 슬롯 발급

---

## 2. 확정된 전제

착수 전 지시서와 현재 코드가 충돌하는 지점을 조사해 확정했다. 아래 설계는 전부 이 전제 위에 있다.

| # | 쟁점 | 확정 | 근거 |
|---|---|---|---|
| 1 | 팔레트 적용 범위 | `redesign-b` 위에서 작업 | 잉크+시그널이 이미 89개 파일에 전역 이식됨(미배포). 홈만 바꾸면 톤이 갈라짐 |
| 2 | 재방문 이어보기 | 기록 훅까지 구현 | `saved-shoes.ts`(찜) 외에 최근 본/비교/추천 기록 코드가 없음 |
| 3 | 러닝화 인덱스 | 화면 16행 + 나머지는 `<details>` | 현행 122켤레 전체 렌더로 41,722px |
| 4 | 전체 목록 페이지 | **신규 생성 안 함** | 필터를 쓰는 목록 경로가 `home-content.tsx` 하나뿐 — 전체 목록 페이지가 곧 홈 |
| 5 | 고민별 탐색 목적지 | `/best/*` 큐레이션 | 6개 고민이 전부 실존 slug에 매핑됨. 고RPM 페이지로 세션 이동 |
| 6 | 고민 항목 수 | 6개 (§16의 5개 + 무릎 부담) | 무릎은 검색 수요가 크고 `/best/knee-protection`이 실존 |
| 7 | 히어로 모션 빈도 | 세션당 1회 | 홈은 1인당 3~4회 경유하는 순회 허브 |
| 8 | 신발 와이어프레임 | 실제 제품 실루엣의 윤곽선 추출 | 손으로 쓴 SVG path 3회 실패(배·카누 형태). 4절 참조 |
| 9 | 배포 단위 | 팔레트 전체 + 홈 구조 | 토큰만 바꿔도 89파일이 함께 변함. 톤 불일치 기간을 만들지 않음 |
| 10 | 확산 로드맵 | 상세 → 베스트/vs → 기능 → 콘텐츠 | 수익 기여도 순 |

### 4번이 설계에 미친 영향

`/shoes`를 신규 생성하지 않으므로 **필터는 홈에 그대로 남는다**(지시서 §20 원안 충족).
인덱스는 정적 HTML에 122행을 전부 렌더하되 16행 초과분을 `<details>`로 접는다.

- 내부링크 122개가 정적 HTML에 그대로 남아 SEO 손실 없음
- 화면상 스크롤은 16행 분량
- 새 URL 0개
- `<details>`라 자바스크립트 없이도 펼쳐지고 크롤러가 내부를 읽음

---

## 3. 팔레트 전환

승인된 Warm Performance는 `redesign-b`에 이미 적용된 잉크+시그널의 미세 조정판이다.

```css
:root {
  --paper:       #F7F4ED;   /* redesign-b: #FCFBF9 */
  --paper-soft:  #EFEAE0;   /* redesign-b: #F7F5EE (--bg-warm-alt) */
  --surface:     #FFFFFF;   /* 동일 */
  --ink:         #17150F;   /* 동일 */
  --ink-soft:    #6E6A5E;   /* redesign-b: #5F5B4E (--secondary) */
  --line:        #D9D4C8;   /* redesign-b: #E3E0D6 (--border) */
  --signal:      #FF4D00;   /* 동일 */
  --signal-soft: #FFE7DB;   /* redesign-b: #FFF0E8 (--accent-soft) */
}
```

`src/app/globals.css`의 값 5개를 조정하면 89개 파일이 함께 전환된다.
`tailwind.config.ts`가 `sky`/`slate` 스케일을 오버라이드해둬서 raw 클래스를 쓰는 파일도 코드 수정이 필요 없다.
스케일 값(`sky.50`~`sky.950`, `slate.50`~`slate.950`)은 새 `--paper`/`--line`에 맞춰 함께 미세 조정한다.

### 색 사용 규칙

- 밝은 배경 70~80% / 잉크 영역 20~30% / 시그널은 제한적
- **오렌지 솔리드 채움은 페이지 전체에서 CTA 2곳(히어로·최종 밴드)에만 허용**한다.
  나머지 오렌지 용법은 선·텍스트·테두리 강조뿐이라 CTA가 유일하게 면으로 튀어나온다.
- 다크 영역은 최종 CTA 밴드 한 곳. 전체 세로 픽셀 대비 22% 이하
- 새 유채색 추가 금지
- **`prefers-color-scheme` 기반 자동 다크 테마는 홈에서 제거**한다(`prefers-reduced-motion`은 유지)

### 타이포

- 한글 제목·본문·UI: Pretendard 또는 시스템 산세리프. 대형 제목은 800 이상
- 수치·라벨·상태: mono. **한글 본문에는 쓰지 않는다**
- `word-break: keep-all`, 제목에 `text-wrap: balance`, 숫자 정렬에 `tabular-nums`
- 한글 세리프 금지

---

## 4. 히어로

### 4.1 구성과 제약

- 데스크톱 최대 100svh, 모바일 65~75svh(강제하지 않음)
- 스크롤 가로채기·핀 스크롤 금지. CTA는 연출 중에도 즉시 클릭 가능
- 자동 연출 4.5초(모바일 2.4초) 후 종료. 세션당 1회(`sessionStorage`)
- 주인공은 특정 제품이 아니라 브랜드와 분석 시스템

텍스트는 지시서 원문 그대로 사용한다. 키커 `KOREAN RUNNER FIT INDEX`,
메인 카피 `감으로 고르지 / 않는다.`(둘째 줄만 시그널), 보조 문구, 주요 CTA `약 1분 · 9문항 맞춤 추천`,
CTA 보조 정보, 보조 CTA `고민별로 찾아보기`(고민별 탐색으로 스크롤).

### 4.2 러닝 에너지 — 사선 트랙

분석 60 : 에너지 40의 인상을 만들기 위해 수평 중심 구성을 사선으로 바꾼다.

- **트랙 레인**: 배경에 `-9°` 평행선 5줄. 아래로 갈수록 진해져 원근을 만든다
- **데이터 기준선**: 4줄을 같은 `-9°`로. 각 선 끝에 시그널 캡
- **라벨 진입**: 왼쪽에서 사선 방향으로 흘러들어와 기준선에 정렬.
  기울어진 좌표계 안에 두되 라벨 자체는 역회전시켜 텍스트를 수평으로 유지한다(가독성)
- **속도선**: 실루엣 힐 뒤로 뻗는 시그널 선 3줄
- **스타트 라인**: 좌측에 얇은 사선 1줄 + `START` 라벨

카피 영역에도 사선 요소를 최소한으로 둔다(키커 앞 짧은 시그널 선, 로고 옆 수직선).

### 4.3 모션 타임라인

| 구간 | 장면 | 내용 |
|---|---|---|
| 0.0–0.8s | 복잡한 시장 | 제품명 4개 + 수치 라벨 8개가 흩어진 채 등장. 어느 제품도 가장 크지 않다 |
| 0.8–1.8s | 같은 기준으로 정렬 | 수치가 무게·드롭·발볼·용도 4개 기준선으로 이동. 제품명은 여기서 사라진다 |
| 1.8–2.8s | 와이어프레임 형성 | 러닝화 윤곽선이 왼쪽에서 오른쪽으로 드러난다(전진 방향과 동일) |
| 2.8–3.6s | 한국 러너의 발 | 점선 발 윤곽 중첩 + 발볼 계측 틱 3개 |
| 3.6–4.5s | 적합 영역 → CTA | 겹침 구간이 시그널로 짧게 밝아졌다 가라앉고 시선이 CTA로 이동 |
| 이후 | 정지 | 데스크톱 `pointer:fine`에서만 2~6px 시차. **카피와 CTA는 움직이지 않는다** |

제품명 라벨은 실존 모델만 쓴다(`NOVABLAST 6` `PEGASUS 42` `SC ELITE V5` `GEL-KAYANO 33` — 전부 DB 확인됨).
최종 잔류 라벨은 4개(`249g` `8mm DROP` `WIDE 76mm` `DAILY`).

### 4.4 러닝화 와이어프레임 생성

손으로 쓴 SVG path는 3회 실패했다(칼라 홈이 없으면 배처럼 보이고, 넣으면 뿔처럼 튀어나옴).
**실제 제품 이미지의 알파 채널에서 윤곽선만 추출하는 방식**으로 확정한다.

빌드 시 1회 생성해 정적 자산으로 커밋한다(런타임 처리 아님):

```js
// 1) 투명 여백 제거 후 리사이즈
const png = await sharp(src).trim({threshold: 8}).resize(460, null, {fit:'inside'}).png().toBuffer();
// 2) 알파를 블러 후 이진화 — 아웃솔 러그·로고 같은 식별 디테일을 뭉갠다
const a = await sharp(png).extractChannel(3).blur(2.2).raw().toBuffer();
for (let i = 0; i < a.length; i++) a[i] = a[i] > 132 ? 255 : 0;
// 3) 종이색 실루엣으로 합성 (파일 약 2.5KB)
```

화면에서는 종이색 실루엣에 4방향 시그널/잉크 그림자를 걸어 균일한 윤곽선만 남긴다:

```css
filter: drop-shadow( 1.4px 0 0 var(--ink)) drop-shadow(-1.4px 0 0 var(--ink))
        drop-shadow(0  1.4px 0 var(--ink)) drop-shadow(0 -1.4px 0 var(--ink));
```

원본은 호카 클리프톤 10이다. 블러+이진화로 러그·로고 등 식별 디테일은 제거되어
일반적인 러닝화 옆모습만 남지만, **형태의 출처가 특정 모델이라는 점은 기록해 둔다**(운영자 승인 완료).
채워진 제품 사진이 아니라 선으로만 존재하는 도면이므로 §12의 "제품 사진을 중앙에 크게 배치" 금지에는 해당하지 않는다.

### 4.5 히어로 비주얼 비중

카피·타이포 35~40% / 숫자·데이터 25~30% / 선·실루엣 20~25% / 실제 제품 이미지 5~10%.
히어로에 채워진 제품 사진은 0장이다. 실제 썸네일은 인덱스 행에서만 쓴다.

---

## 5. 섹션 맵

신규 방문자 기준 순서다. **재방문자는 03에 이어보기가 삽입되고 05(추천 작동 방식)가 빠진다** —
이미 아는 사용자에게 9문항 설명을 반복하면 이어보기와 인덱스 사이 거리가 멀어져 순회 효율이 떨어진다.
추천 진입은 헤더 CTA와 최종 밴드로 여전히 두 번 열려 있다.

| # | 컴포넌트 | 목적 / 유도 행동 | 데이터 연결 |
|---|---|---|---|
| 01 | `HomeHeader` | 5개 메뉴 + 검색 + 추천 CTA | `getSearchIndex()` — 서버에서 슬림 prop 전달(기존 패턴 유지) |
| 02 | `KineticHero` | 5초 안에 전문성 각인 / **9문항 추천 시작** | 없음(라벨은 정적 상수, 실존 모델명만) |
| 03 | `ResumeSection` | 중단된 탐색 재개 / 최근 본·비교·지난 추천 | `localStorage` — 신규 기록 훅 3곳 + `saved-shoes` |
| 04 | `ProblemExplorer` | 고민에서 출발 / `/best/*` 이동 | 고민→slug 매핑 상수 + 매트릭스 결과 수 |
| 05 | `RecommendationProcess` | 1분·9문항의 부담 없음 전달 / 추천 시작(2차) | `questionnaire.tsx` 문항 수를 읽어 산출 |
| 06 | `TrustMetrics` | 출처 공개, 직접 실측과 구분 / 기준 펼쳐보기 | `getShoes().length` · 최신 글 발행일 |
| 07 | `ShoeIndexPreview` | 데이터 두께 제시 / **상세 이동(수익 60~70%)** | `getGridShoes()` — 카테고리 균형 정렬 |
| 08 | `HomeAdSlot` | 기존 수익 유지 / 없음 | `ADSENSE_SLOTS.homeBottom`(변경 없음) |
| 09 | `EditorialSection` | 수치가 못 말하는 판단 / 블로그 이동 | `getPostsCardMeta()` |
| 10 | `FinalRecommendationCTA` | 이탈 직전 전환 / 추천 시작·전체 보기 | 없음 |
| 11 | `HomeFooter` | — | — |

하위 컴포넌트: `HeroDataLabel` `HeroShoeOutline` `HeroFootOverlay` `ProblemOption`
`RecommendationStep` `FitIndicator` `DataSourceBadge` `ShoeIndexRow` `ResumeItem`
`EditorialLead` `EditorialListItem`

### 5.1 고민별 탐색 (6개)

| # | 고민 | 표시할 판단 기준 | 목적지 |
|---|---|---|---|
| 01 | 처음 러닝화를 고르고 있어요 | 쿠션 여유, 무게, 가격대 | `/best/beginner` |
| 02 | 편안하게 오래 달리고 싶어요 | 스택 높이, 충격흡수 성향, 로커 구조 | `/best/max-cushion` |
| 03 | 발볼이 넓어서 선택이 어려워요 | 와이드 출시 여부, 토박스 너비, 갑피 여유 | `/best/wide-toebox` |
| 04 | 무릎 부담이 걱정돼요 | 충격흡수 성향과 안정성 구조 | `/best/knee-protection` |
| 05 | 기록을 줄이고 싶어요 | 플레이트 유무, 무게, 에너지 리턴 | `/best/racing` |
| 06 | 한 켤레로 다양하게 뛰고 싶어요 | 다목적 성향, 내구성 범위, 가격 균형 | `/best/daily-trainer` |

04는 **기능성 표현으로만** 서술한다 — "충격흡수 성향과 안정성 구조를 참고해 정리합니다.
진단이나 치료 목적이 아닙니다." 치료·통증 완화·부상 예방을 단정하지 않는다.
섹션 하단에 면책 문구를 둔다: "러닝화 정보는 제품 선택을 위한 참고 자료이며, 통증의 진단이나 치료를 대신하지 않습니다."

카드 설명 영역에는 `min-height`를 줘 하단 라인을 맞춘다(설명 길이 차이로 어긋나는 것 방지).

### 5.2 러닝화 인덱스

기본 정렬은 **카테고리 균형**이다. rating 단순 정렬은 상위가 전부 30만원대 레이싱 슈퍼슈즈라
홈 기본값으로서 대표성이 없다. 배분: 데일리 4 · 쿠션화 3 · 입문화 3 · 레이싱 2 · 안정화 2 · 트레일 2.

행 구성은 **7칸 그리드**다: 썸네일 · 브랜드+모델 · 한 줄 결론 · 용도 · 무게 · 가격 · 데이터 상태.
(구현 시 그리드 칸 수와 자식 요소 수를 반드시 일치시킬 것 — 시안에서 6칸으로 잡아 배지가 다음 줄로 밀리는 버그가 있었다.)

필터 칩: 전체 · 카테고리 6종 · 와이드 출시 · 20만원 이하. 선택 시 결과 수를 `aria-live`로 알린다.

데이터 상태 배지는 6종(`공식 정보` `외부 랩 확인` `리뷰 종합` `직접 착화` `추정치` `갱신 대기`)을 쓰고
`외부 랩 확인`만 시그널 색으로 강조한다. 색만으로 구분하지 않고 텍스트를 함께 둔다.

> **클래스명 주의**: 배지에 `.lab`을 쓰지 말 것. 히어로 데이터 라벨의 `.lab`(absolute·opacity 0)과
> 충돌해 배지가 통째로 사라진다. `.verified` 등 다른 이름을 쓴다.

---

## 6. 반응형

데스크톱 축소가 아니라 모바일을 별도 설계한다.

| 항목 | 데스크톱 | 모바일 (≤900px) |
|---|---|---|
| 히어로 높이 | 최대 100svh | 65~75svh, 강제 없음 |
| 모션 길이 | 4.5s | 2.4s |
| 라벨 수 | 12개 → 4개 잔류 | 4개(잔류 라벨만) |
| 정렬 연출 | 기준선 4개로 이동 | 생략 — 등장 후 바로 최종 배치 |
| 스타트 라인·속도선·기준선 태그 | 표시 | 숨김 |
| 마우스 시차 | 2~6px | 없음 |
| 고민별 탐색 | 3열 × 2행 | 1열 6행 |
| 인덱스 | 16행 / 7칸 | 12행 / 3칸 압축(썸네일·모델·가격 + 무게·상태) |
| 필터 | 칩 인라인 | 바텀시트(ESC 닫힘, 포커스 트랩) |
| 저사양 기기 | — | 정적 inline SVG로 대체 |

**모바일 첫 화면**에 키커·메인 카피·보조 문구·주요 CTA가 스크롤 없이 들어간다.
비주얼은 CTA 아래에 두되 **실루엣 기준 약 15% 확대**하고 위 여백을 줄여 러닝화 단서가 빨리 보이게 한다.

확대는 컨테이너 폭만 키우면 라벨과 신발이 좌우로 잘리므로 두 값을 나눠 적용한다:

| | 데스크톱 | 모바일 | 실루엣 실폭 |
|---|---|---|---|
| 비주얼 컨테이너 | 100% | 106% (`margin-left: -2%`) | — |
| 실루엣 위치·폭 | `left 26% / width 70%` | `left 22% / width 76%` | — |
| **합산 배율** | `1.00 × 0.70 = 0.70` | `1.06 × 0.76 = 0.806` | **+15%** |

여백은 `aspect-ratio`를 `560/400`에서 `560/280`으로 줄이고 `margin-top: -10px`를 줘 CTA에 붙인다.
컨테이너를 118%까지 키우면 라벨 왼쪽과 신발 앞코가 화면 밖으로 나가므로 이 조합을 넘기지 않는다.

확인 폭: 1440 / 1280 / 1024 / 768 / 430 / 390 / 360.

---

## 7. 광고

첫 배포에서 디자인과 광고 조건을 동시에 바꾸지 않는다.

- 슬롯 수 1개 유지, `ADSENSE_SLOTS.homeBottom` **ID 변경 없음**, 상단 이동 금지, 추가·제거 금지
- 위치는 구조적으로 동일하다: 현행 "결과 그리드 ↔ Beyond Shoes" → 개편 후 "인덱스 ↔ 에디토리얼".
  같은 경계를 승계한다
- 인덱스가 122행에서 16행으로 줄어 광고 도달률은 오히려 오른다(현재는 광고에 닿기 전 대부분 이탈)
- 광고 앞뒤에 CTA를 두지 않는다. 금지 영역(내비·히어로·CTA 주변·탐색·검색·필터·이어보기) 전부 회피
- 높이 예약 280px 유지(CLS), 미노출 시 공간 축소, 카드처럼 꾸미지 않고 `Advertisement` 라벨 표시
- 홈에서 Auto ads 임의 삽입 제한

---

## 8. GA4 이벤트

`gtag`가 `layout.tsx`에만 있고 이벤트 유틸이 없으므로 얇은 래퍼를 새로 만든다.

```
home_recommend_start      home_problem_category_click   home_shoe_detail_click
home_best_click           home_compare_click            home_blog_click
home_resume_click         home_ad_view                  home_filter_apply
home_search_start
```

파라미터: `device_type` `visitor_type`(신규/재방문) `section_name` `item_name`
`destination_path` `filter_name` `filter_value`

---

## 9. 접근성 · 성능 · SEO

**접근성** — `prefers-reduced-motion`에서 자동 모션 제거(최종 화면 즉시 표시) ·
모든 버튼과 필터 키보드 접근 · 명확한 `focus-visible` · 오렌지만으로 상태 구분 금지 ·
터치 영역 44px 이상 · 장식 SVG `aria-hidden` · 결과 수 `aria-live` · 바텀시트 ESC 닫힘 ·
모바일 메뉴 포커스 트랩 · **애니메이션 종료 전에도 CTA 클릭 가능**

**성능** — Three.js·WebGL·Canvas·외부 애니메이션 라이브러리 금지 ·
CSS 애니메이션과 inline SVG 중심 · 히어로 전용 JS 200KB 미만 · 스크롤 잠금/속도 변경 금지 ·
이미지 lazy loading · IntersectionObserver 기반 진입 · 애니메이션 완료 후 클래스 고정 ·
이미지 크기 사전 지정 · 광고 높이 예약 · CI 페이로드 가드 통과

**SEO** — 히어로 카피를 실제 HTML로(이미지 아님) · 링크는 실제 `<a>` · SSG 유지 ·
JS 실패 시에도 핵심 카피와 링크 노출 · `<h1>` 하나 · 인덱스 122행 정적 렌더로 내부링크 보존 ·
신발 개수와 갱신일은 공통 데이터 소스에서 동적 산출(**하드코딩 금지**) ·
구조화 데이터는 실제 콘텐츠와 일치. `aggregateRating`/`review`(Person 저자)는 넣지 않는다

---

## 10. 상태 디자인

공통: loading · empty · error · hover · focus · active · selected · disabled

- **이어보기**: 기록 있음 / 일부만 있음 / 기록 없음(섹션 자체를 렌더하지 않음) / 유효하지 않은 URL(안전하게 제외) / 삭제 완료
- **필터**: 결과 있음 / 없음 / 다중 적용 / 초기화 / 바텀시트 열림·닫힘
- **이미지**: 로딩 / 오류 / 대체 / 이미지 없는 항목
- **광고**: 로딩 / 표시 / 미노출 / 차단 환경

---

## 11. 데이터 표현 원칙

allrunabout은 자체 랩 실측을 하지 않는다. 아래 표현을 홈 어디에도 쓰지 않는다.

> 같은 랩에서 직접 측정 · 자체 실측 122종 · 자체 연구소 검증 · 모든 모델 Lab Verified ·
> 의학적으로 검증된 부상 예방 · 통증 치료 · 근거 없는 92% 적합도 · 검증되지 않은 종합점수

권장 표현은 "여러 출처를 같은 기준으로 정리", "공식 정보와 전문 랩 데이터를 종합",
"한국 러너 관점으로 재해석"이며, 데이터 상태 6종으로 출처를 구분해 표시한다.

추천 결과의 적합도는 가짜 정밀 점수 대신 항목별 등급(발볼·러닝 목적·예산·국내 구매)으로 표현하고,
색상만으로 구분하지 않는다.

---

## 12. 검증 기준

구현 후 실측해 확인한다. 말로 하는 다짐이 아니라 숫자로 판정한다.

| 항목 | 기준 | 시안 실측 |
|---|---|---|
| 홈 전체 높이 | 8,000px 이하 (현행 41,722px) | **6,343px** |
| 다크 영역 비중 | 전체 세로 픽셀의 22% 이하 | 최종 CTA 밴드 1곳 |
| 히어로 최종 잔류 라벨 | 4개 | 4개 |
| 인덱스 행당 수치 | 2개 + 상태 배지 1개 | 충족 |
| 홈에서 나가는 링크 | best 6 · 상세 122(16 노출) · 추천 3 · 비교 1 · 블로그 5 · 마라톤 1 | 구현 후 카운트 |
| 오렌지 솔리드 CTA | 페이지 전체 2곳 | 히어로 · 최종 밴드 |
| 히어로 제품 사진 | 0장 | 0장 |

배포 후 가드레일: 홈 이탈률 +2~3%p 이내 · 수익 페이지 이동률 -5% 이내 ·
세션당 페이지뷰 유지 · 추천 시작률 유지 또는 증가 · 홈 경유 세션 전체 수익 유지 또는 증가.
홈 직접 광고 수익이 소폭 줄어도 홈 경유 세션 전체 수익이 유지되면 허용한다.

---

## 13. 배포 계획과 확산 로드맵

### 1단계 (이번 작업)

`redesign-b`를 최신 `main`과 머지한 뒤(현재 2커밋 뒤처짐, 신발 데이터 충돌 예상) 작업한다.
팔레트 토큰 조정으로 89개 파일이 함께 전환되고, 레이아웃 재설계는 홈만 한다.
상세·비교·베스트는 기존 레이아웃에 새 색만 입은 상태로 나간다.
톤이 갈라지는 기간이 없고, 문제 발생 시 원인이 "홈 구조"로 좁혀진다.

### 후속 단계 (수익 기여도 순)

| 단계 | 대상 | 비고 |
|---|---|---|
| 2 | 신발 상세 | 전체 수익의 60~70%. `redesign-b`가 이미 21개 파일을 손댔으므로 검수·보완 수준 |
| 3 | 베스트 / vs | pSEO 고RPM |
| 4 | 비교 / 추천 | 기능 페이지 |
| 5 | 블로그 / 마라톤 | 콘텐츠 |

각 단계 배포 후 GA·AdSense로 가드레일을 확인하고 다음 단계로 넘어간다.

### 확산할 것과 홈에 남길 것

**확산한다** — 데이터 상태 배지(홈에서 "외부 랩 확인"이라 해놓고 상세에 표기가 없으면 신뢰 체계가 어긋난다) ·
mono 수치 표기와 `tabular-nums` · 플랫 조판(그림자 0, radius 2px 이하) · 행 기반 목록(나열형 페이지)

**홈에 남긴다** — 사선 트랙 · 키네틱 모션 · 실루엣 와이어프레임.
홈은 관문이라 강한 인상이 필요하지만 상세·비교는 읽고 판단하는 도구라 조용해야 한다.
상세 배경에 사선 트랙을 깔면 수치 읽기를 방해한다. 타협이 아니라 의도된 분리다.

---

## 14. 리스크

| 리스크 | 대응 |
|---|---|
| 인덱스 16행 노출로 홈 내부링크 감소 | 122행을 정적 HTML에 전부 렌더하고 `<details>`로 접어 링크를 보존 |
| 팔레트 전환이 89파일에 동시 적용 | `redesign-b`에서 이미 검증된 브릿지 구조. 배포 전 전 페이지 육안 검수 |
| `blue-*`·`rgba(14,165,233)` 등 raw 값 잔재 | 브릿지가 안 먹는 케이스. 배포 전 grep으로 전수 확인 |
| 히어로 모션이 재방문 순회를 방해 | 세션당 1회 + 연출 중 CTA 클릭 가능 |
| 와이어프레임 원본이 특정 모델 | 블러+이진화로 식별 디테일 제거, 선 도면으로만 사용(운영자 승인) |
| 이어보기 기록 훅이 홈 밖 3개 페이지를 건드림 | 각 페이지 3~5줄 추가, 레이아웃 변경 없음 |
| 빌드 검증 누락 | `npm run validate && npm run build`를 `set -o pipefail`과 함께 실행(과거 `| tail`이 실패 코드를 삼켜 깨진 채 배포된 이력) |

---

## 15. 참고 자산

- 고충실도 시안: `.omc/design/home-mockup-2026-07-31.html` (데스크톱·모바일, 실제 데이터·썸네일 내장)
- §35 설계 제안서: `.omc/design/home-redesign-proposal-2026-07-31.html`
- 팔레트 원안: `.omc/design/design-b-ink-signal.html`
